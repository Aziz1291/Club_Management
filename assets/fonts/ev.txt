<script>
    function openEventModal(eventId = null) {
        const modal = document.getElementById('eventModal');
        const modalTitle = document.getElementById('modalTitle');
        const form = document.getElementById('eventForm');
        
        if (eventId) {
            modalTitle.textContent = 'Edit Event';
            // Here you would populate form with existing event data
            // For now, we'll just set the event ID
            document.getElementById('eventId').value = eventId;
        } else {
            modalTitle.textContent = 'Add New Event';
            form.reset();
            document.getElementById('eventId').value = '';
        }
        
        modal.style.display = 'block';
    }
    
    function closeEventModal() {
        document.getElementById('eventModal').style.display = 'none';
    }
    
    function editEvent(eventId) {
        openEventModal(eventId);
        // Here you would fetch event data and populate the form
    }
    
    function deleteEvent(eventId) {
        if (confirm('Are you sure you want to delete this event?')) {
            // Here you would send delete request to server
            alert('Event deleted successfully!');
        }
    }
    
    document.getElementById('eventForm').addEventListener('submit', function(e) {
        e.preventDefault();
        
        const formData = new FormData(this);
        const eventId = formData.get('event_id');
        
        if (eventId) {
            alert('Event updated successfully!');
        } else {
            alert('Event created successfully!');
        }
        
        closeEventModal();
    });
    
    window.onclick = function(event) {
        const modal = document.getElementById('eventModal');
        if (event.target === modal) {
            closeEventModal();
        }
    }
</script>
event
<script>
    // Add interactivity for buttons
    document.querySelectorAll('.btn-attend').forEach(button => {
        button.addEventListener('click', function() {
            const eventTitle = this.closest('.event-card').querySelector('.event-title').textContent;
            alert(`Thank you for registering for: ${eventTitle}`);
            this.innerHTML = '<i class="fas fa-check"></i> Registered';
            this.style.background = '#64748b';
            this.disabled = true;
        });
    });

    document.querySelectorAll('.btn-details').forEach(button => {
        button.addEventListener('click', function() {
            const eventTitle = this.closest('.event-card').querySelector('.event-title').textContent;
            const eventDescription = this.closest('.event-card').querySelector('.event-description').textContent;
            alert(`Event Details:\n\n${eventTitle}\n\n${eventDescription}`);
        });
    });
</script>

<?php include("header.php"); ?>
<style>
    .events-page {
        padding: 40px 20px;
        background: linear-gradient(135deg, #f8fafc 0%, #e2e8f0 100%);
        min-height: 100vh;
    }

    .page-header {
        text-align: center;
        margin-bottom: 50px;
    }

    .page-title {
        font-size: 3rem;
        font-weight: 700;
        color: #1e293b;
        margin-bottom: 15px;
        background: linear-gradient(135deg, #3b82f6, #9b51e0);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
    }

    .page-subtitle {
        font-size: 1.2rem;
        color: #64748b;
        max-width: 600px;
        margin: 0 auto;
    }

    .events-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
        gap: 30px;
        max-width: 1200px;
        margin: 0 auto;
    }

    .event-card {
        background: white;
        border-radius: 20px;
        padding: 30px;
        box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
        transition: all 0.3s ease;
        border: 1px solid #f1f5f9;
        position: relative;
        overflow: hidden;
    }

    .event-card::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        height: 4px;
        background: linear-gradient(135deg, #3b82f6, #9b51e0);
    }

    .event-card:hover {
        transform: translateY(-10px);
        box-shadow: 0 20px 40px rgba(0, 0, 0, 0.15);
    }

    .event-header {
        margin-bottom: 20px;
    }

    .event-title {
        font-size: 1.5rem;
        font-weight: 700;
        color: #1e293b;
        margin-bottom: 10px;
        line-height: 1.3;
    }

    .event-date {
        display: inline-flex;
        align-items: center;
        gap: 8px;
        background: linear-gradient(135deg, #3b82f6, #2563eb);
        color: white;
        padding: 8px 16px;
        border-radius: 20px;
        font-size: 0.9rem;
        font-weight: 600;
    }

    .event-description {
        color: #64748b;
        line-height: 1.6;
        margin-bottom: 25px;
        font-size: 1rem;
    }

    .event-details {
        display: flex;
        flex-direction: column;
        gap: 12px;
        margin-bottom: 25px;
    }

    .event-detail {
        display: flex;
        align-items: center;
        gap: 12px;
        color: #475569;
        font-size: 0.95rem;
    }

    .event-detail i {
        width: 20px;
        color: #3b82f6;
        font-size: 1.1rem;
    }

    .event-admin {
        display: flex;
        align-items: center;
        gap: 12px;
        padding: 15px;
        background: #f8fafc;
        border-radius: 12px;
        border-left: 4px solid #3b82f6;
    }

    .admin-avatar {
        width: 40px;
        height: 40px;
        border-radius: 50%;
        background: linear-gradient(135deg, #3b82f6, #9b51e0);
        display: flex;
        align-items: center;
        justify-content: center;
        color: white;
        font-weight: 600;
        font-size: 1rem;
    }

    .admin-info h4 {
        margin: 0;
        color: #1e293b;
        font-size: 1rem;
        font-weight: 600;
    }

    .admin-info p {
        margin: 0;
        color: #64748b;
        font-size: 0.85rem;
    }

    .event-actions {
        display: flex;
        gap: 12px;
        margin-top: 20px;
    }

    .btn-attend {
        flex: 1;
        background: linear-gradient(135deg, #10b981, #059669);
        color: white;
        border: none;
        padding: 12px 20px;
        border-radius: 10px;
        font-weight: 600;
        cursor: pointer;
        transition: all 0.3s ease;
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 8px;
    }

    .btn-attend:hover {
        transform: translateY(-2px);
        box-shadow: 0 8px 20px rgba(16, 185, 129, 0.3);
    }

    .btn-details {
        background: #f1f5f9;
        color: #475569;
        border: none;
        padding: 12px 20px;
        border-radius: 10px;
        font-weight: 600;
        cursor: pointer;
        transition: all 0.3s ease;
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 8px;
    }

    .btn-details:hover {
        background: #e2e8f0;
        transform: translateY(-2px);
    }

    .no-events {
        text-align: center;
        padding: 60px 20px;
        color: #64748b;
    }

    .no-events i {
        font-size: 4rem;
        margin-bottom: 20px;
        color: #cbd5e1;
    }

    .no-events h3 {
        font-size: 1.5rem;
        margin-bottom: 10px;
        color: #475569;
    }

    @media (max-width: 768px) {
        .events-grid {
            grid-template-columns: 1fr;
            gap: 20px;
        }
        
        .page-title {
            font-size: 2.5rem;
        }
        
        .event-card {
            padding: 25px;
        }
    }
</style>

<div class="events-page">
    <div class="page-header">
        <h1 class="page-title">Upcoming Events</h1>
        <p class="page-subtitle">Join our exciting events and connect with the community. Discover upcoming activities and register your participation.</p>
    </div>

    <div class="events-grid">
        <?php
        // Sample events data - replace with your actual database data
        $events = [
            [
                'id' => 1,
                'title' => 'Tech Innovation Summit 2024',
                'description' => 'Join us for an exciting day of technology innovation, featuring keynote speakers, workshops, and networking opportunities with industry leaders.',
                'date' => '2024-02-15',
                'location' => 'Grand Conference Center',
                'admin' => 'Sarah Johnson',
                'admin_role' => 'Event Coordinator'
            ],
            [
                'id' => 2,
                'title' => 'Community Charity Run',
                'description' => 'Participate in our annual 5K charity run to support local education initiatives. All fitness levels welcome!',
                'date' => '2024-02-22',
                'location' => 'Central Park',
                'admin' => 'Mike Chen',
                'admin_role' => 'Sports Director'
            ],
            [
                'id' => 3,
                'title' => 'Art & Culture Exhibition',
                'description' => 'Experience breathtaking artworks from local artists. Live demonstrations and interactive sessions included.',
                'date' => '2024-03-05',
                'location' => 'Modern Art Museum',
                'admin' => 'Emma Rodriguez',
                'admin_role' => 'Cultural Manager'
            ],
            [
                'id' => 4,
                'title' => 'Business Networking Breakfast',
                'description' => 'Connect with professionals from various industries over breakfast. Perfect for expanding your professional network.',
                'date' => '2024-03-12',
                'location' => 'Skyline Hotel',
                'admin' => 'David Kim',
                'admin_role' => 'Networking Lead'
            ]
        ];

        foreach ($events as $event) {
            $formattedDate = date('F j, Y', strtotime($event['date']));
            $adminInitials = getInitials($event['admin']);
            
            echo "
            <div class='event-card'>
                <div class='event-header'>
                    <h3 class='event-title'>{$event['title']}</h3>
                    <span class='event-date'>
                        <i class='fas fa-calendar-alt'></i>
                        {$formattedDate}
                    </span>
                </div>
                
                <p class='event-description'>{$event['description']}</p>
                
                <div class='event-details'>
                    <div class='event-detail'>
                        <i class='fas fa-map-marker-alt'></i>
                        <span>{$event['location']}</span>
                    </div>
                    <div class='event-detail'>
                        <i class='fas fa-clock'></i>
                        <span>9:00 AM - 5:00 PM</span>
                    </div>
                </div>
                
                <div class='event-admin'>
                    <div class='admin-avatar'>{$adminInitials}</div>
                    <div class='admin-info'>
                        <h4>{$event['admin']}</h4>
                        <p>{$event['admin_role']}</p>
                    </div>
                </div>
                
                <div class='event-actions'>
                    <button class='btn-attend'>
                        <i class='fas fa-check-circle'></i>
                        Register Now
                    </button>
                    <button class='btn-details'>
                        <i class='fas fa-info-circle'></i>
                        Details
                    </button>
                </div>
            </div>
            ";
        }
        
        function getInitials($name) {
            $names = explode(' ', $name);
            $initials = '';
            foreach ($names as $n) {
                $initials .= strtoupper($n[0]);
            }
            return substr($initials, 0, 2);
        }
        ?>
    </div>
</div>



<?php include("footer.php"); ?>